server:
    port: 8088

info:
    build:
        name: ${project.artifactId}
        version: ${project.version}
        time: ${git.build.time}

spring:
    cloud:
        config:
            uri: http://localhost:8888/config
            failFast: true
            name: mycfg
            