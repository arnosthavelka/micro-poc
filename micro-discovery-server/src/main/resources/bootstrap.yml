info:
    build:
        name: '@project.artifactId@'
        version: '@project.version@'
        time: '@git.build.time@'

spring:
    application:
        name: ${info.build.name}
    cloud:
        config:
            uri: http://localhost:8888/config
            failFast: true
