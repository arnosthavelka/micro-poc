server:
    port: 8761

info:
    component: Discovery server

spring:
    application:
        name: discovery-server
    cloud:
        config:
            name: discovery
            
eureka:
    client:
        # standalone mode
        registerWithEureka: false
        fetchRegistry: false
    server:
        waitTimeInMsWhenSyncEmpty: 0