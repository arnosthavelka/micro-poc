spring:
    jpa:
        hibernate:
            ddl-auto: none
      
liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.sql
    
logging:
    level:
        com.github.aha.poc.micro: 'DEBUG'

eureka:
    client:
        enabled: false