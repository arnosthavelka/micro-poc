server:
    port: 8090

spring:
    cloud:
        config:
            failFast: true
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: none
      
liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.sql
    
logging:
    level:
        com.asseco.aha.poc.micro: 'DEBUG'
