server:
    port: 8091

info:
    id: ${spring.application.name}
    build:
        name: ${project.artifactId}
        version: ${project.version}

spring:
    application:
        name: ${project.artifactId}
#    cloud:
#        config:
#            name: mycfg
