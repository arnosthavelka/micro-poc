server:
    port: 8888

info:
    build:
        name: '@project.artifactId@'
        version: '@project.version@'
        time: '@git.build.time@'

spring:
    profiles:
        active: native
    cloud:
        config:
            uri: http://localhost:8888/config
            server:
                prefix: /config
        